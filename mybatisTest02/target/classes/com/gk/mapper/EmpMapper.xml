<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gk.mapper.EmpMapper">
    <!--
    1 接口的名字和Mapper映射为文件名字必须保持一致(不包含拓展名)
    2 Mapper映射文件的namespace必须是接口的全路径名
    3 sql语句的id必须是对应方法的名
    4 DeptMapper映射文件应该和接口编译之后放在同一个目录下
    -->
    <!--List<Emp> findAll();-->
    <select id="findAll" resultType="emp" >
        select * from emp
    </select>

    <select id="findByEmpNo" resultType="emp" parameterType="int">
        select * from emp where empno = #{empno}
    </select>


    <!--
     多个基本数据类型作为方法参数
     List<Emp> findByDeptnoAndSal(@Param("detpno") int deptno,@Param("sal") double sal);
     方式1 arg*     arg0 arg1 arg2 数字是索引,从0开始

     方式2 param*   param1 param2 param3 数字是编号,从1开始
     使用别名
     List<Emp> findByDeptnoAndSal(@Param("detpno") int deptno,@Param("sal") double sal);
     通过@Param注解使用别名之后,就不能再使用arg* 但是可以继续使用param*
    -->

    <select id="findByDeptAndSal" resultType="emp">
        <!-- select * from emp where deptno = #{arg0} and sal &gt; #{arg1} -->
        <!-- select * from emp where deptno = #{param1} and sal >= #{param2} -->
        select * from emp where deptno = #{a} and sal >= #{b}
    </select>

    <!--参数是map可以使用阻断名字即可-->
    <select id="findByDeptAndSal2" resultType="emp" parameterType="map">
        select *
        from emp
        where deptno = #{deptno}
          and sal >= #{sal};
    </select>

    <select id="findByDeptAndSal3" resultType="emp" parameterType="emp">
        select *
        from emp
        where deptno = #{deptno}
          and sal >= #{sal};
    </select>

    <!--
        List<Emp> findByDeptAndSal4(Emp empa, Emp empb);
        arg*
        param*
        别名也可以 @Param("a")
    -->
    <select id="findByDeptAndSal4" resultType="emp" parameterType="emp">
        <!--select *
        from emp
        where deptno = #{arg0.deptno}
          and sal >= #{arg1.sal};-->
        select *
        from emp
        where deptno = #{param1.deptno}
        and sal >= #{param2.sal};
    </select>

    <!--模糊查询-->
    <select id="findByDeptAndSal5" resultType="emp">
        select *
        from emp
        where ename like concat('%',#{ename},'%')
    </select>

    <insert id="addEmp">
        insert into emp values (#{empno},#{ename},#{job},#{mgr},#{hiredate},#{sal},#{comm},#{deptno});
    </insert>

    <update id="updateEmpByEmpno" >
        update emp set ename=#{ename} where empno=#{empno};
    </update>

    <delete id="deleteEmpByEmpno" >
        delete from emp where empno = #{empno}
    </delete>

</mapper>
